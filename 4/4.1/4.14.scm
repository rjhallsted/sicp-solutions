;Louis' map does not work because the first argument to map is a procedure.
;The supplied procedure has to be applied to the arguments by the evaluator,
;as it is represented as a list and not a procedure in the underlying scheme.